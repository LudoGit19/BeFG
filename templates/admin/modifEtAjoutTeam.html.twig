{% extends 'base.html.twig' %}

{% block title %}Modification de l'équipe{% endblock %}
{% block monTitre %}Equipe {{ team.name }}{% endblock %}

{% block body %}
{% include "header2.html.twig" %}



<div class="container-fluid"> 
    {{form_start(form)}}    
            <div class="row">
                <div class="col-6 p-4 m-auto">
                    <div>{{ form_row(form.imageFile) }}</div> 
                {{form_widget(form)}}
                    <a href="{{ path('admin') }}" class="btn btn-primary">Retour</a>
                    <input type="submit" value="Valider" class="btn btn-success">
                </div>
            </div>
        </div>    
    {{form_end(form)}}
</div>    
     
        
{% endblock %}
